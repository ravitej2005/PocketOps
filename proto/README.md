# PocketOps Protocol Buffers

This directory will contain the shared gRPC/Protobuf contract (`pocketops_agent.proto`) between the Go Agent and Spring Boot backend.

The actual protocol definition is implemented in Phase 4.
